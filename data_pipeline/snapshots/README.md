# Saved snapshots

Generated snapshot JSON files are ignored by default. Preserve a snapshot with
its source URL, resource ID, dataset modification time, observation time,
retrieval time, units, and quality flags whenever results need to be reproduced.

The NESO demand dataset is public operating context, not a live network state.
